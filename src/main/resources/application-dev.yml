

server:
  port: 8081

spring:

  thymeleaf:
    suffix: .html
    mode: HTML
    cache: false
    servlet:
      content-type: text/html

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/DemoSystem?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true&setUnicode=true&characterEncoding=utf8
    username: root
    password: 12345678

